name: Continuous Delivery  # Corrigido nome do workflow

on:
  push:
    branches:
      - main  # Corrigido espaço antes do nome da branch

jobs:
  deliver:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Simulando Continuous Delivery
        run: echo "Simulando deploy..."  # Corrigido erro de sintaxe

      - name: Upload Projeto Zipado
                - uses: actions/upload-artifact@v4
                with:
                  name: projetozipado.zip
                  path:
                    - '**/*.py'   # ✅ Correto! Agora está como lista
                    - README.md








      
        
                    
                    





